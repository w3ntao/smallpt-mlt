# build and run

```
# clone this repository and its submodules
$ git clone --recurse-submodules https://github.com/w3ntao/smallpt-mlt.git

# build
$ cd smallpt-mlt
$ mkdir build; cd build
$ cmake ..; make -j

# render
$ ./smallpt-mlt
```
